# Wedge Ethiopia

Initial repository anchor. The complete V1 code tree is staged in `.bootstrap/wedge-source.tar.gz` and is expanded, verified, and committed by the bootstrap workflow on pushes to `main`.
